- name: Print course variable from vars_file
  hosts: localhost
  connection: local

  vars_files:
    - course_vars.yaml

  tasks:
    - name: 
      ansible.builtin.debug:
        msg: "Hello {{ name }}, welcome to the {{ course }} course!"