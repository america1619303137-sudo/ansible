- name: print names
  hosts: web
  connection: local
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "Hello {{ item }}"
      loop:
        - modda
        - lanja
        - bewas

