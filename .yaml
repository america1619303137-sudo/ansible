- name: print names
  hosts: web
  connection: local
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: hello raa gaandu "{{names}}"
        loop:
          - modda
          - lanja munda
          - bewas munda
