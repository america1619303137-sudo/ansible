- name: convert to lower case
  hosts: local
  connection: local
  tasks:
    vars:
      name: "MANGAL SINGH LOVE MANGAL SINGH"
  ansible.builtin.debug:
  msg: "lower case format is  {{ name | upper}}"