- name: identify filters
  hosts: localhost
  connection: local
  # vars:
  #   person: laude
  tasks:
    - name: filters
      ansible.builtin.debug:
        msg: "kyaa re {{ person | default ('RANDI')}}"
